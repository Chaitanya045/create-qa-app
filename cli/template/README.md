# CLI Templates

This directory is reserved for file-based scaffolding assets.

Current template generation is driven by `cli/src/template/manifest.ts`.
As the scaffold matrix grows, static template files should be added here and wired through the template resolver/renderer.
