# {{projectName}}

Generated by `create-qa-app`.

## Stack

- Framework: {{frameworkLabel}}
- Architecture: {{architectureLabel}}
- Language: TypeScript
- Runtime validation: {{runtimeValidationLabel}}
- Reporting:
{{playwrightSelectedReportersList}}

## Getting Started

{{playwrightGettingStartedSteps}}

## Environment

- `.env` is loaded automatically via `dotenv` whenever the generated config reads environment values.
- `.env.example` is safe to commit and should document the variables your project expects.
- `.env` is ignored by git so secrets stay local.
- In CI, configure the same variables through your CI provider secrets instead of committing a real `.env` file.

## Mise

{{playwrightMiseSetupSection}}

## Quality Commands

- `{{formatCheckCommand}}` checks formatting.
- `{{lintFixCommand}}` auto-fixes lint issues where possible.

## Playwright Commands

- `{{playwrightRunTestsCommand}}` runs the end-to-end tests.
- `{{playwrightUiModeCommand}}` starts interactive UI mode.
- `{{playwrightProjectCommand}}` runs tests only on Desktop Chrome.
- `{{playwrightDebugCommand}}` runs tests in debug mode.
- `{{playwrightCodegenCommand}}` auto-generates tests with Codegen.

## Report Commands

{{playwrightReportCommandsList}}

## Architecture Notes

- Page Object Model (POM) keeps page abstractions separate from tests for long-term maintainability.
